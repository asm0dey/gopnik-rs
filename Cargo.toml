[package]
name = "gopnik"
version = "0.1.0"
edition = "2021"
rust-version = "1.97"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
